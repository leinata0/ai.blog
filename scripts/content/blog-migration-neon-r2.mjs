export default {
  title: '把博客从易丢库状态迁到 Neon + R2：一次架构重做的完整复盘',
  slug: 'blog-migration-neon-r2-cloudflare-render-vercel',
  summary: '从 Render Free + SQLite 到 Neon + R2 的一次完整博客架构迁移复盘。',
  tags: ['devops', 'cloudflare', 'neon', 'render', 'vercel'],
  cover_prompt: 'Wide landscape banner, cinematic, high quality: a personal tech blog architecture migration scene, glowing arrows moving from a fragile local sqlite setup into a modern cloud stack with Vercel frontend, Render backend, Neon Postgres, Cloudflare R2 and a Cloudflare shield in front, blue and cyan lighting, clean dashboard screens, professional engineering aesthetic',
  content_md: String.raw`## 一、问题是怎么暴露出来的

这次迁移不是“为了追新技术”，而是被真实故障逼出来的。

我原来的博客链路本质上是一个很典型的个人项目组合：前端放在 Vercel，后端跑在 Render，数据库先用 SQLite 顶着，图片也先走本地上传。刚开始这样做非常顺手，开发快、成本低、心智负担也小，适合先把博客跑起来。

但真正上线跑了一段时间之后，问题开始集中暴露出来：文章会丢、图片会失效、部署一改就容易把旧数据带没，甚至连“到底是数据库没了，还是只是当前实例没挂载到原来的文件”都不容易第一时间判断。最扎心的一次，是我回退代码版本、重部署服务之后，发现之前生成过的文章基本都不见了。

这时候我才彻底意识到一件事：**个人博客可以轻量，但不能建立在“数据和图片随时可能丢”的前提上。**

## 二、旧架构为什么不适合继续跑

旧架构最大的问题，不在某一个服务本身，而在于“关键状态”放错了地方。

### 1. SQLite 放在不稳定运行环境里，天然有风险

SQLite 本身不是问题。对低并发的个人博客来说，SQLite 其实很好用，零运维、迁移简单、本地开发体验也很舒服。真正的问题在于：我当时跑的是 Render Free 方案，而这类环境并不适合承载需要稳定持久化的数据文件。

一旦容器重建、实例切换、部署过程发生偏差，\`blog.db\` 这种文件型数据库就很容易出现“你以为它还在，其实你连到的是一份新的空库”的情况。表面上服务是活的，健康检查也正常，但 \`/api/posts\` 返回空数组，这种感受非常真实地告诉你：代码还在，内容没了。

### 2. 图片走本地上传，和服务实例耦合太深

图片最开始走本地上传时，逻辑确实最简单：上传后返回 \`/uploads/...\`，前端直接渲染就行。但这条链路的稳定性完全绑定后端实例。

只要后端实例重建、挂载路径不一致、或者前端代理和后端地址不同步，就会出现这些问题：

- 图片地址保存下来了，但实际文件已经不在当前实例上
- 前端看起来“上传成功”，刷新后却是裂图
- 后台图片管理里能看到记录，但用户端访问不到

对于博客来说，图片虽然不是第一位，但它一旦不稳定，就会直接拉低整站可信度。

### 3. 前后端分离之后，旧链路残留特别容易误导排障

这次迁移里还有一个很典型的坑：前端虽然已经部署成功，但 \`vercel.json\` 里的 \`/api/*\` 和 \`/uploads/*\` 仍然重写到旧的 API 域名。结果就是：

- 你以为自己在调当前 Render 服务
- 实际上前端还在打旧链路
- 后端环境变量明明改了，页面效果却完全对不上

这种问题最难的地方在于，它不是“整个站直接挂掉”，而是会给你一种“好像半成功、半失败”的错觉。

## 三、新架构怎么设计

最终我把链路重构成了下面这套组合：

| 层级 | 方案 | 角色 |
|------|------|------|
| 前端 | Vercel | 博客页面、后台界面、前端部署 |
| 后端 | Render | FastAPI API、管理接口、图片代理 |
| 数据库 | Neon Postgres | 文章、设置、评论等核心持久化数据 |
| 图片存储 | Cloudflare R2 | 封面图、站点图片、上传图片对象存储 |
| 域名与流量入口 | Cloudflare | DNS、代理、中转与访问稳定性 |

这个架构的核心原则非常清晰：**把真正重要的状态，放到专门做持久化的服务里。**

### Neon 负责数据库持久化

我把数据库从 SQLite 迁到 Neon Postgres 之后，最直观的好处就是：文章、设置、后台数据终于和应用实例解耦了。后端可以重部署、可以滚动更新、可以重建镜像，但数据不会再跟着应用容器一起漂移。

对个人博客来说，Postgres 不一定比 SQLite “更先进”，但在云上部署场景里，它确实更适合做长期在线的数据底座。

### Cloudflare R2 负责图片持久化

图片这块我最终选的是 Cloudflare R2。原因也很直接：

- 成本友好，适合个人项目
- 配合 Cloudflare 自定义域名很顺手
- 和“博客图片长期可访问”这个需求非常贴合

现在新上传的图片会直接落到 R2，然后通过 \`https://img.563118077.xyz/...\` 这种稳定地址访问。这样无论后端实例怎么变，图片都不会跟着一起丢。

### Cloudflare 继续做中转和统一入口

我本来就用了 Cloudflare 做域名中转，这次迁移反而让我更确定这一步是对的。对于这类“前端在 Vercel、后端在 Render、对象存储在 R2”的组合来说，Cloudflare 不只是个 DNS 面板，而是整个外部访问体验的统一入口。

它带来的价值不只是代理本身，还包括：

- 自定义域名整合
- HTTPS 与证书自动化
- 访问路径更稳定
- 后续加缓存、规则、图片域名管理都更顺手

## 四、迁移过程中真正踩过的坑

这次迁移不是一把梭成功的，中间踩了不少非常具体的坑。

### 1. \`DATABASE_URL\` 不是“填上就行”，格式不对直接起不来

迁移到 Neon 后，我第一轮部署就遇到了 SQLAlchemy 的启动报错：数据库 URL 无法解析。后来排查发现，问题并不复杂，但很典型：

- 连接串可能还是 \`postgresql://\`
- 也可能复制时带了引号、空格、换行
- 甚至可能把 \`DATABASE_URL=...\` 整行一起粘进去了

后面我专门把后端的环境变量解析逻辑做了加固，自动兼容这些常见粘贴格式，部署才真正稳下来。

### 2. R2 根域名 404 不代表没配好

另一个特别容易误判的点，是 \`https://img.563118077.xyz\` 打开后显示 404。

一开始我也以为是图片域名没通，后来才确认：**R2 根路径返回 404 是正常的**，真正应该验证的是具体对象地址，比如：

\`https://img.563118077.xyz/xxx.jpeg\`

当这类具体文件能打开时，说明 R2 链路就是通的。

### 3. 上传成功但刷新后消失，不一定是存储挂了

这次我还遇到过一个很隐蔽的问题：后台图片管理里刚上传能看到，刷新后却消失。最后发现不是 R2 挂了，而是上传时生成的文件名没有扩展名，列表逻辑按后缀过滤图片，结果自己把刚上传的文件过滤掉了。

这个问题后来也做了修复：上传时会根据 MIME 自动补扩展名，同时图片列表不再把这种文件误判成无效对象。

### 4. “上传成功”和“保存成功”是两步

站点设置页里还有一个体验层的小坑：上传头像或 Hero 图后，只是先把 URL 写进表单。如果不继续点“保存设置”，刷新之后它还是会消失。

这件事看起来像 bug，但本质上是后台交互语义没讲清楚。也正因为这次迁移，我对“真正持久化发生在哪一步”这件事更敏感了。

## 五、最终效果怎么样

这次架构切换完成后，博客的“稳定性预期”已经完全变了。

以前我最担心的是：

- 这次部署会不会把文章搞没
- 这张图是不是过两天就裂了
- 回退代码之后数据还在不在

现在我更愿意关心的是内容本身，而不是基础设施会不会突然背刺。

最终链路可以概括成一句话：

**Vercel 负责前端体验，Render 负责 API 服务，Neon 承载数据，Cloudflare R2 承载图片，Cloudflare 负责把这一切稳定地串起来。**

从结果看，这套组合对个人技术博客是非常合适的：

- 成本可控
- 维护复杂度没有高到不可接受
- 数据和图片真正具备了长期持久化能力
- 后续继续扩展自动写作、封面生成、正文插图也更安心

## 六、这次迁移之后，我对博客基础设施的判断

这次迁移给我最大的收获，不是“学会了哪家云服务怎么配”，而是把一件事情想得更明白了：

**博客真正值得保护的，不是页面代码，而是你持续积累出来的内容资产。**

代码仓库当然重要，但对一个长期写作的博客来说，更关键的是三样东西：

- 文章数据库
- 图片资源
- 域名与访问入口

只要这三样东西是稳定的，前端可以换，后端可以重构，部署平台也可以调整；反过来，如果这三样东西不稳，那页面再好看也只是暂时的。

所以如果现在让我重新给个人博客排优先级，我会这样排：

1. 先保证数据和图片的持久化
2. 再保证链路和域名的统一
3. 最后才是持续打磨前端样式和自动化体验

这次从“容易丢库、容易丢图”的状态，迁到 \`Vercel + Render + Neon + R2 + Cloudflare\`，本质上不是单纯升级技术栈，而是在给博客搭一套更适合长期写作的底层结构。

对我来说，这一步非常值。

> 架构升级真正带来的安心感，不是它用了多少新服务，而是你终于知道：下一次部署，不会再把自己写过的东西弄丢。`,
}
